spring:
  application:
    name: server-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/roseJoke/bloomRose/   #我的git远程仓库地址
          username: roseJoke                                #用户名
          password: 100715xy                                #密码
          search-paths: Repo-config                         #配置仓库路径下的相对搜索位置，可配置多个
eureka:
  client:
    serviceUrl:
      defaultZone: http://alice:1234@172.17.0.20:7555/eureka/
server:
  port: 7888

