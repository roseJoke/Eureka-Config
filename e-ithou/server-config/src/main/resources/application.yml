spring:
  application:
    name: server-config
  cloud:
    config:
      server:
        svn:
          uri: https://github.com/roseJoke/bloomRose.git    //我的远程仓库地址
          username: roseJoke
          password: 100715xy
          search-paths: https://github.com/roseJoke/bloomRose.git